# intro to variables.
name = input("What is your name? ")
# displaying variable
print("Welcome " + name + "!")
# we can calculate the length by using len().
book = input("What's the name of your favourite novel? ")
length = len(book)
print(length)
